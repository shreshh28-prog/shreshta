lst = []
size = int(input('enter the array size'))
for i in range(size):
    ele = int(input('enter array elements:'))
    lst.append(ele)

def Parti(arr,low,high):
    arr[high] = pivot
    i = low - 1
    for j in range(low,high):
        if arr[j]<=pivot:
            i = i + 1
            arr[i+1],arr[j] = arr[j],arr[i+1]
    arr[i+1],arr[high] = arr[high],arr[i+1]
    return i + 1

def Quicksort_arr(arr,low,high):
    if low<high:
        pi = parti(arr,low,high)
        Quicksort_arr(arr,low,pi-1)
        Quicksort_arr(arr,pi+1,high)
    return arr

print('sorted array is :',Quicksort_arr(lst,0,size-1))

























