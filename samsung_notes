
i++
j--
a[i] = b[j]
b[j]++

a[++i] = b[--j]++
sum = x++ + ++x


x = 10
y = x

Arithmatic operators:
+ - * // / % **

2 ** 3 ** 3

1. Python is Dynamically typed language
2. Why there are no ++ and -- operators in Python ?
3. how range function  works?
4. Why in Python you have no function overloading?

n = 12
for i in range(n):
	print(i)
	i += 2

range(n)
range(m, n)
range(m, n, i)
range(n, m , -i)

	
0
2
4
6
8
10

len(var)
name.count()

(10, 20) open interval from 11 to 19
[15, 30] closed interval   from 15 to 30
(5, 45] left open     from 6 to 45
[start, end)   range function

for i in my_range(n):
	print(i)
	
myRange()

n = 20
num = 20
age = 20
studentAge = 20

notepad++
git
git desktop
vs code
python
mysql

Data Structures

int num1 = 50, num2 = 80..............;

int numbers[50];

array [45, 'bmsce', 50.5]

Data Structures in Python

list: mutable, ordered
class is the rules, solution, definitions


Customer
	id
	name
	dob
	phoneNumber
	email
	address
	adhaar
	pan
	
BankAccount
	id
	customerId
	accountNumber
	balance
	
	createAccount()
	retrieveAccount()
	updateAccount()
	deleteAccount()
	listAccounts()
	
	freezeAccount()
	
Transaction

list
	array
	append()
	extend()
	pop()
	insert()
	sort()

https://github.com/neelmyna/bms_may26

git commands:

1. configuration
	git config --global user.name "neelmyna"
	git config --global user.email "abcd@gmail.com"

2. Copy (clone)
	git clone https://github.com/neelmyna/bms_may26
The above command will connect to internet and copy the repo folder from the github(server) into our system (laptop). So we should be seeing a folder by the name of the repo.


git clone repo_url
git clone https://github.com/username/repo_name

git clone https://PAT@github.com/username/repo_name

khjg359pqytihgfjhgkdfjjklsv

git clone https://khjg359pqytihgfjhgkdfjjklsv@github.com/neelmyna/bms_may26

create github account
create new repo (public)
create PAT and save it
configuration 2 commands
clone the repo
clone the repo with PAT

PUSH and PULL
push is to copy the files and folders from local repo to remote repo
PULL is the opposite of PUSH

Push:
	listing the files and folders ()
	create the commit object with necessary messages
	push
	
Remember, each commit is a version.
Its not necessary to push every commit. We can push multiple commits at a time.

In our repo folder these are content:	
30 old (not modified) + 15 new (5 each in a sub folder, such 2 sub-folders) + 2 del (5 to be ignored)

Git will list these:
	10 new files
	2 deleted files
------------------------------------------
Analysing the Linear Search Algorithm.

The solution for Linear Search is Brute Force Technique.
Brute Force is the technique that come usually naturally without having to apply any specific Math rule or formulea.
Thus there is no inventor or author for these solutions.

The best case scenario is when the element is found at index zero.
Thus there would only one search.
Thus the BCE (best case efficiency) of Linear Search is O(1)  Read it as: Big-Oh of One

If in case the element is not found or it is found at the last possible index (last position) then it is the Worst case scenario and thus the WCE would be O(n) Where 'n' is size of the list.

If in case the element is found some where at the mid of the list, then can coin the term Avg Case Efficiency and that would be O(n)

CHARACTERISTICS OF BIG-OH:
1. It is used usually to find WCE. However, we can also find the other 2.
2. The Big-Oh always depends on the i/p size and gives the efficiency in order of the i/p size, say 'n'.
3. The Big-Oh is here to tell us the Order in which the algorithm is. It's duty is Not to give the exact efficiency of the algorithm.
4. The Big-Oh gives the efficiency in terms of 'n'
5. The Big-Oh uses the Math functions such as factorial, logarthimic, sq-root etc.
--------------------------------------------
BINARY SEARCH:

Strategy: Divide and Conquer
What it is used for: To search an element in a list (An array and not a linked list)
Pre-requisite: It expects the List to be sorted fully
Solution/Algorithm:
	Go to mid element of the list and check if it is the search
	If yes,
		then the search can stopped
	If not, 
		then check if the element is less than the mid element (i.e. if the element is in 1st half od the list)
		if yes,
			then set the search area to 1st half of the list
		if not,
			set the search area to the 2nd half
	Repeat the above steps until at least one element is left

Pseducode:
	do this:
		mid_position = (1st position + last position) // 2
		if element == list[mid_position] then
			stop the search and if you wish, return mid_position
		else
			if element < list[mid_position] then
				list = 1st half of list
			else:
				list = 2nd half of list

Code:


Analyse the efficiencies:
BCE is O(1)
WCE is 




Think Optimisation:


Conclude/Infer:
----------------------------------------
UNDERSTANDING COMMAND LINE ARGUMENTS:

import sys

print(type(sys.argv))
print(sys.argv)
print(sys.argv[0])
print(sys.argv[1])
print(sys.argv[2])
print(type(sys.argv[2]))
print(sys.argv[22])
---------------------------------------------
Assume number of iterations of the while loop is X

n    n/2     n/4  ........   8      4      2     1
2(X-1)                      2(3)    2(2)  2(1)  2(0)

2 pow (X-1) = n
2 pow X = n
X = O(Log N)

100000		1
50000		2
25000		3
12500		4
6250		5
3125		6
1562		7
781			8
390			9
195			10
97			11
48			12
24			13
12			14
6			15
3			16
1			17
-------------------------------------------------
Brute Force: Bubble sort, selection sort
Decrease and Conquer: Insertion sort
Divide and Conquer: Merge sort, quick sort
Tree Based Sorting: BBST sort, heap sort
Combination of algorithms Sorting: Bucket Sort
Non comparitive sorting: Radix Sort

Bubble Sort

77   34   55   45    9   13   22   35
34   55   45    9   13   22   35   77
34   45    9   13   22   35   55   77

n-1 + n-2 + n-3 .... 3 + 2 + 1
O(n2)

9   12   22   34   35   45   55   77
9   12   22   34   35   45   55   77

O(n2)

Pseudocode:
for i from 0 to n-2 do:
	for j from 0 to n-2-i do:
		if list[j] > list[j+1]:
			swap list[j], list[j+1]

BCE O(n2)
WCE O(n2)

for i from 0 to n-2 do:
	sorted = true  # assume the list is sorted
	for j from 0 to n-2-i do:
		if list[j] > list[j+1]:
			swap list[j], list[j+1]
			sorted = false
	if sorted:
		break

BCE O(n)
WCE O(n2)
------------------------------------------------
Insertion Sort:

77   34   55   45    9   13   22   35   99
34   77
34   55   77
34   45   55   77
 9   34   45   55   77
 9   13   22   34   35   45   55   77
 
for i in range(n):
	element = numbers[i]
	j = i - 1
	while j >= 0 and element < numbers[j]:
		numbers[j+1] = numbers[j]
		j -= 1
	numbers[j+1] = element
	
9   13   22   34   35   45   55   77
9   13   22   34   35

BCE of Insertion sort is O(n)

9   13   22   34   35   45   55   77
9
13   9
22  13    9

WCE of Insertion sort is O(n2)

-----------------------------------------------
SELECTION SORT:

9   13   55   45    77   34   22   35   99

element = 13
index = 5

ALGORITHM:
	How the algorithm works
	What strategy it uses
	Write the algorithm
	Psedocode
	Check the flow (flow chart)
	Code
	Discuss Efficiencies
	Optimization
	Application (where to use and where and when not to use)
	Industry use
----------------------------------------------------
Tisha went to her orchard and plucked a bunch of oranges. Now she wants to keep aside some of the oranges to for herself and her children but also sell the biggest ones on the market. She takes a look at the last orange in her hand and decides that she would like to eat it. She seems to think it fair that all the oranges smaller than the orange in her hand can go to her children. All the oranges bigger than this one will be sold on the market. Write a program to help Tisha do this partition. Assume the oranges are neatly stacked in a row.

numbers = []
pivot = numbers[-1]
i = 0   # to access each element of the list
j = 0 # to know/find the index of the pivot element

for i in range(len(numbers)-1):
	if numbers[i] < pivot:
		numbers[i], numbers[j] = numbers[j] numbers[i]
		j += 1
numbers[-1], numbers[j] = numbers[j] numbers[-1]

The Partition if partitions the array in 2 equal halves then the further number of calls to the partition algorithm would be lesser.

Contrarily, if we are taking the worst case scenario (where we sorting the rray in opposite order) then the number of partitions would linearly grow/increase.

Partition has already a fixed efficiency of O(n)
The increase in call to the partition() would increase the effic of quick_sort() from Log(n) to 'n'

Thus, the WCE of Quick sort is O(n²)
------------------------------------------
DAY7 13-05-2026 WEDNESDAY

MERGE SORT:
STRATEGY: Divide and Conquer

DIVIDE STEP: Divide the given array recursively into smallest possible chunks of arrays

CONQUER STEP: Merge the arrays RECURSIVELY and during the merge, compare and then add element from the arrays to be merged, say A and B into the merged array, say C.

divide algorithm and merge algorithm

77  34  55  45  9  13  22  35  99
77  34  55  45       9  13  22  35  99
77  34      55  45       9  13  22  35  99
77      34      55  45       9  13  22  35  99
77      34      55       45       9  13  22  35  99
77      34      55       45       9  13      22  35  99
77     34     55      45     9     13      22  35  99
77     34     55      45     9     13      22      35  99
77     34     55      45     9     13      22      35     99
                                            

34    77       45    55      9    13       22    35      99
                                  

34  45   55   77     9   13   22   35          99

9   13    22   34   35   45    55   77       99
i											  j

9   13    22   34   35   45    55   77   99
k

BCE nlogn
WCE nlogn

stable sorting technique


PSEUDOCODE:
def divide_array(numbers, low, high):
	if low < high:
		mid = (low + high) // 2
		divide_array(numbers, low, mid-1)
		divide_array(numbers, mid+1, high)
		merge(numbers[low:mid+1], numbers[mid+1:]) 

def merge(array_a, array_b):
	merged_array = []
	i = j = 0
	while i < len(array_a) and j < len(array_b):
		if array_a[i] < array_b[j]:
			merged_array.append(array_a[i])
			i += 1
		else:
			merged_array.append(array_b[j])
			j += 1
	merged_array.extend(array_a[i:])
	merged_array.extend(array_b[j:])

with open(file_name, in_mode) as fp:
	do the things here
---------------------------------------------
TREE
* A Tree is non-linear DS
* A Tree is acyclic DS
* A Tree has one entry point namely ROOT
* The data in a Tree is stored in its NODES
* The Nodes in the tree are connected
* The Root is also a Node
* From a Node the nodes we can reach immediately (because they are connected, say, via an edge) are called as child nodes. A node in a tree can have any number of child nodes including zero.
* A Node with zero child nodes is said to be LEAF Node or Brahmachari Node.
* When we reach a child node from the current node, it is said to ASCEND the Tree once.
* Since we have the terminology Child Node, the PARENT node is best possible term.
* The Root is said to be at a level 1 and its immediate child at level 2 and so on.
* The connected edges from the node we start ascending towards the node we have reached is said to be a PATH in the TREE.
* Thus, level of the leaf node in a PATH is the HEIGHT of that Path in the Tree.
*  The level of the leaf node in the longest path is the HEIGHT of the tree.
* Synonym for Level is DEPTH.
*** Height of a node in a tree is the height of the path from it to the leaf node.
* All paths in the Tree are unique! Because the tree is acyclic(heirarchical).
* If in Tree, every node have at most 2 child nodes then such a tree is said to be BINARY TREE (BT)
* For a given BT of height N, the maximum number of nodes of the tree is (2 pow n) - 1  (Note that is an Odd number). Only the level 1 has odd number of nodes, which is the Root itself.
* In a BT, if the number of nodes is exactly 2powN - 1, then it is said to be COMPLETE BT.
* If in a BT, all the nodes have exactly 0 or 2 child nodes (only), then such BT is said to be STRICTLY BT.
* Since in BT every node can have at most 2 nodes, we can name them as LEFT CHILD NODE and RIGHT CHILD NODE.
* The Tree which ascends through (passes through) the left child node is said to be LEFT SUB TREE and thus we have RIGHT SUB TREE.
*** If in a BT, the nodes are ARRANGED in such a way that the data in every node is greater than the data in its left child node and less than the data in its right child node, then such a BT is said to be BINARY SEARCH TREE (BST).
* A BT or BST is said to be BALANCED, if the diffrence between the left sub tree height and the right sub tree height is nearly zero. In other words if the BT has nodes which is nearer to 2powN-1 then the BT is said be balanced.
* In a BST if at all levels, the tree is balanced, then any of the CRUD operations (expect the delete) has an efficiency of O(LogN)
* If in a BST if all nodes have only left child nodes or only right child nodes, then the efficiency of any of the CRUD operations is O(n) which is the worst case efficiency for a BST.
* We can traverse a BSTR in 3 diffrent order.
	1. Inorder traversal (LR'R): Access the left child then the root and lastly the right child. This must be recursively done with every new node we access.
	2. Pre-Order (R'LR): Access the root then the left and lastly the right
	3. PostOrder (LRR'): Left then right and lastly Root
* If any of the 2 traversals are given, we can build the BST:
	1. Inorder and pre-order
	2. InOrder and PostOrder
----------------------------------------------
Linked List:
Linear Linked lists:
	1. Singly LL (straight chain, circular)
	2. Doubly LL

Problems we can solve on SLL/DLL:
1. Sort the linked list (Bubble sort)
2. Reverse a singly linked list (1. iteratively 2. recursively)
3. Check if 2 linked lists converge at some point of time
4. Merge 2 sorted linked lists into a one sorted list.
5. Break a linked list into w of even and odd elements.
6. Implement the following operations on a singly linked list:
 1. Insert at front
 2. insert at rear
 3. insert at position
 4. Delete from position
 5. Delete from front
 6. Delete from rear
 7. Traverse Singly linked list forward (front to towards rear)
 8. Diaplay nodes of Singly linked list backwards (rear to front)
-----------------------------------------------------
Operations on BST:

Adding a new node:
We always a new node as leaf node.

Write Menu driven program to implement inserte, update and search and list elements of a BST.

There are 2 types of relationships
1. Generalisation (Inheritance)
	is-a relationship or type-of relationship
2. Association (we associate one object with other)
	has-a or part-of relationship
	Container containee relationship
	1. Composition (strong association)
	2. Aggreggation (weak association)
-----------------------------------------------------
Check if tree is empty
Check if tree has only the root and to be deleted
Check if the node being deleted is leaf node
Check if the node being deleted has one child
Check if the node being deleted has two children (applies to the tree where we are deleting the root)

linuxfoundation.org

sem3:  s1   s2   s3   s4   s5
sem4:  s1   s2   s3   s4   s5
sem5:  s1   s2   s3   s4   s5
sem6:  s1   s2   s3   s4   s5
sem7:  s1   s2   s3   s4   s5
sem8:  s1   s2   s3   s4   s5


def check_arrangement(boys, girls):
	if boys[0] > girls[0]:
		for i in range(len(boys):
			if girls[i] <= boys[i]:
				continue
			else:
				return 'no'
	elif boys[0] <= girls[0]:
		for i in range(len(boys):
			if boys[i] <= girls[i]:
				continue
			else:
				return 'no'
	return 'yes'

Read N     #number of test cases
	results = []
	for i from 1 to N do:
		Read M 	   #number of boys and girls
		Read boys[M]
		Read girls[M]
		sort both the lists
		
		result = check_arrangement(boys, girls) # returns 'yes' or 'no'
		results.append(result)
	print results
	
g  3  4  6
b  2  3  5 

2 3 3 4 5 6

ml = 3  2  4  3  6  5
sml= 2  3  4  5  6  7


merged_list = [item for pair in zip(boys, girls) for item in pair]
------------------------------------------------------
What is an exception?
We want something to be done which cannot be done. This situation during RUNTIME is said to create an exception.
eg:
1. We are trying to modify a file but mutating the file is not allowed.
2. We are trying to connect to a DB but it fails (may be password wrong, or server busy or may be has been put off)
3. We are trying to access 10th element in a list but it has only 6 elements.

Technically what happens when an exception occurs?
An object of a specific exception class is created and stored in memory and its reference value is thrown by the PVM.

If this reference value is not caught (some instructions with in the code), then the PVM handles the exception by stopping/killing the process/App and returns/prints the error.

Since the error returned by PVM is technical in nature and is not understood by the user, and also, since the execution stops, we may have to do something about it.

-> Hence, we use the exception handling.
We define the TRY block and put the instructions which where we SUSPECT the exception to occur.
-> And it is now our duty to give the solution for the exception(s) that we suspect. This is done by defining the except blocks.
-> The except blocks receive reference value of the exception object which are defined out of claases. And we know, the classes may be related via inheritance.
-> Hence the except blocks must be defined in opposite order of their derivation. This is because, the matching of the reference value of the exception object happens in order of the definitions of the except blocks.
-> Thus, the 1st except block defined among the many must be the block that receives the reference value of most specialized type (child most class in the heirarchy)

-> We can optionally define a finally block which would run always irrespective of the exception being occured or not.
-> We can have instructions out of the try block but defined before the try block.
-> We cannot have instructions that are nither part of try nor except blocks which are placed in between the try and except blocks.
-> For one try block you must define at least one except block.
-> We can define our own exception class which must inherit some existing exception class.
-> We can explicitly raise an exception.
-> We can propogate an exception to the caller. By doing so, we are saying that we do not want to handle the exception.
-> If nobody handles the exception (callee nor any of the callers) then it will be ultimately hanldled by the PVM.
-> If can have instructions defined after the except blocks with in the function.
-> We can have another set of try-except-(finally) blocks one after the other with in a function.
-> The except blocks of classes at same level in the hrirarchy can be defined in any order of our choice.
-> We can nest a try block in a try block
-> When the matching of except blocks of nested try block fails, then  the matching of the exception would continue with the except blocks of the parent try block.
------------------------------------------------------
Saturday 23-05-2026

import numpy as np

array1 = np.zeros(3)
print(f'Array1 = {array1}')

array2 = np.zeros((1, 4))
print(f'Array2 = {array2}')

	array3 = np.zeros((2, 5))
	print(f'Array3 = {array3}')
---------------------------------------------
import numpy as np

array1 = np.zeros(3)
array2 = np.zeros((1, 4))
array3 = np.zeros((2, 5))

print(array1[0], array1[2])
print(array2[0][0], array2[0][3])
print(array3[1][0], array3[1][3])
-------------------------------------------------
import numpy as np

array1 = np.zeros(3)
array2 = np.zeros((1, 4))
array3 = np.zeros((2, 5))

print(array1[4]) # IndexError
print(array1[0][0]) # SyntaxError array1 is not 2D array
print(array2[2][0]) # IndexError
print(array2[1][0]) # IndexError
print(array3[3][3]) # IndexError
--------------------------------------------
import numpy as np

array1 = np.zeros(3)
array2 = np.zeros((1, 4))
array3 = np.zeros((2, 5))

print(type(array1))
print(type(array2))
print(type(array1[0]))
print(type(array2[0]))
print(type(array2[0][0]))
----------------------------------------------
import numpy as np

array1 = np.full((2, 4), 5)
print(array1)
array2 = np.full((1,5), 10)
# Here array2 is still a ndarray
print(array2)

array1 = np.full((2, 4), 5, dtype=int)
array1 = np.full((2, 4), 5, dtype=np.float64)
------------------------------------------------
import numpy as np
# arange()

array1 = np.arange(10)
array2 = np.arange(10, 20)
array3 = np.arange(10, 30, 3)

print(type(array1))
print(array1)
print(array2)
print(array3)
------------------------------------------------------
import numpy as np
# ones()

array1 = np.ones(10)
array2 = np.ones((2, 8))
array3 = np.ones((3, 5))

print(type(array1))
print(array1)
print(array2)
print(array3)
---------------------------------------------
import numpy as np

vector = np.arange(5)
print('Vector shape:', vector.shape)

matrix = np.ones([3, 2])
print('Matrix:', matrix)
print('Matrix shape:', matrix.shape)

tensor = np.zeros([2, 3, 3])
print('Tensor:', tensor)
print("Tensor shape:", tensor.shape)
-------------------------------------------------
import numpy as np

arr = np.arange(1, 10)
print(arr, '\n')

# Reshape to 3x3 matrix
arr = arr.reshape(3, 3)
print(arr, '\n')

# Reshape back to the original size
arr = arr.reshape(9) # generates 1D array
print(arr)

arr = arr.reshape(1, 9) # generates 2D array
print(arr)

arr = arr.reshape(2, 5) # ValueError
print(arr)
-----------------------------------------------------
import numpy as np

arr1 = np.arange(12)

arr2 = arr1.reshape(2, 6)
arr3 = arr1.reshape(6, 2)
arr4 = arr1.reshape(3, 4)
arr5 = arr1.reshape(12, 1)
arr6 = arr1.reshape(4, 3)

print('Arr1:\n', arr1)
print('Arr2:\n', arr2)
print('Arr3:\n', arr3)
print('Arr4:\n', arr4)
print('Arr5:\n', arr5)
print('Arr6:\n', arr6)
-------------------------------------------------------
import numpy as np

arr = np.arange(1, 10).reshape(3, -1) # Here python infers/decides the number of columns by itself.
print(arr)
-------------------------------------------------------
import numpy as np

arr1 = np.arange(1, 10) #arange always generates 1D array
arr2 = np.arange(2, 25, 2)
arr3 = arr1.reshape(3, -1) # Reshape generates 1D, 2D and 3D arrays and so on
arr4 = arr2.reshape(4, -1)
arr5 = arr2.reshape(2, -1)
arr6 = arr2.reshape(3, -1)
arr7 = arr2.reshape(-1, 4) # Numpy predicts and fixes number of rows
arr8 = arr2.reshape(-1, -1) #ValueError. Can only specify one unknown dimension

print('Arr1:\n', arr1)
print('Arr2:\n', arr2)
print('Arr3:\n', arr3)
print('Arr4:\n', arr4)
print('Arr5:\n', arr5)
print('Arr6:\n', arr6)
print('Arr7:\n', arr7)
print('Arr8:\n', arr8)
---------------------------------------------------------
import numpy as np

array1 = np.array([1, 3, 5, 0, 2, 3, 4, 5, 13, 17, 23, 29])
print(array1.shape)
print(type(array1))
print(array1)
---------------------------------------
import numpy as np

array1 = np.array([1, 3, 5, 0, 2, 3, 4, 5, 13, 17, 23, 29])

array1.shape = (6, 2)
print(array1.shape)
print(array1)

array1.shape = (3, 4)
print(array1.shape)
print(array1)

array1.shape = (4, 3)
print(array1.shape)
print(array1)

#array1.shape = (4, 2) # Error New shape of the array must consist of same number of elements as that of original array
#print(array1.shape)
#print(array1)
-------------------------------------------------
import numpy as np

matrix1 = np.array([[3, 4, 5], [2, 6, 9]])
matrix2 = np.array([[3, 4], [3, 5], [2, 6]])

matrix3 = np.dot(matrix1, matrix2)

print('Matrix3=\n', matrix3)
(r1*c1, r1*c2)
(r2*c1, r2*c2)

(3*3 + 4*3 + 5*2), (3*4 + 4*5 + 5*6)
(2*3 + 6*3 + 9*2), (2*4 + 6*5 + 9*6)

(9+12+10), (12+20+30)
(6+18+18), (8+30+54)
[ (31, 62)
  (42, 92) ]
---------------------------------------------
import numpy as np

array = np.array([2, 4, 6, 8, 9, 19])

array2 = array + 5 # Broadcasting. Adding a scalar quantity to every element of the array

print(array)
print(array2)
------------------------------------
import numpy as np

array = np.array([[2, 4, 6, 8], [9, 19, 4, 10]])

array2 = array + 5 # Broadcasting. Adding a scalar quantity to every element of the array

print(array)
print(array2)
--------------------------------------
import numpy as np

matrix1 = np.array([[3, 3, 4], [2, 3, 9]])
matrix2 = np.array([[2, 5, 4], [2, 3, 19]])

sum_matrix = matrix1 + matrix2
difference_matrix = matrix1 - matrix2
product_matrix = matrix1 * matrix2
quotient_matrix = matrix1 / matrix2

print(sum_matrix)
print(difference_matrix)
print(product_matrix)
print(quotient_matrix)
------------------------------------------
import numpy as np
import scipy

array = np.array([[1, 1, 3, 3, 4, 4, 4, 5, 7, 7, 8, 9, 12]])

mean   = np.mean(array)
median = np.median(array)
mode   = scipy.mode(array)

print(f'Mean = ', mean)
print(f'Median = ', median)
print(f'Mode = ', mode)
---------------------------------------------
num = 50

while(num < 50):
	# do something
	pass
else:
	# do something else
	pass
---------------------------------------------
To enforce elements of same type to encapsulated, we must use array from the module array
array.array stores elements of same type only.
-----------------------------------------------------

{"airline" : "akasa", "source" : "chennai", "destination" : "jaipur", 
"duration" : 3.25, "fare" : 9200, "id" : 1}

-------------------------------------------------------------
import numpy as np
def f(x, y):
	return 10 * x + y

my_aaray = np.fromfunction(f, (5, 4), dtype = int)
print(my_aaray)
------------------------------------------------------
import numpy as np
def f(x, y):
	return 10 * x + y

my_aaray = np.fromfunction(f, (5, 4), dtype = int)

# Slicing the Numpy Arrays:
print(my_aaray[2, 3]) # my_array[2][3]
print(my_aaray[0:5, 1]) # From Row-1 to Row-5, print the 2nd element
print(my_aaray[ : , 1]) # From all rows, print 2nd element
print(my_aaray[1:3, : ]) # From Row-2 to Row-3, print all elements
-------------------------------------------------------
import numpy as np

import numpy as np
def f(x, y):
	return 10 * x + y

my_array = np.fromfunction(f, (5, 4), dtype = int)

print(f'Before:\n {my_array}')
#my_array[:, [0, -1]] = 0  #For all Rows, set 0 to 1st and last columns
my_array[[0, -1], :] = 0 #For 1st row and last row, set all elements to 0

#After:
print(f'After:\n {my_array}')
----------------------------------------------------
import numpy as np

import numpy as np
def f(x, y):
	return 10 * x + y

my_array = np.fromfunction(f, (5, 4), dtype = int)

my_array[[0, 1, -1], :] = 0 #For 1st row and last row, set all elements to 0

#After:
print(f'After:\n {my_array}')
---------------------------------------------------
Chapter7 Test Link:
https://forms.gle/aKjyhd83ng1eK1UJ7

Student Feedback Link:
https://docs.google.com/forms/d/e/1FAIpQLSfSTyyWEk0XwRgXKmVqrIgntWTN5kxzk7PixTF3UsaDn8AuQg/viewform?usp=publish-editor


Chapter 8 Test Link:
https://forms.gle/yb9pBfZi2sSfZqep8

Trainer Name:  Nithin N
Trainer Number: 9480273090
